import { combineReducers } from 'redux';
import * as reducers from './TBD'; // Change this to file that I create to hold combineReducers

const rootReducer = combineReducers(reducers);

export default rootReducer;
